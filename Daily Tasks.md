## Create a controlled input form (text input, checkbox, select) and show live updates.

## Build a component that toggles visibility of text (conditional rendering).

## Convert a class-based component to a functional component using hooks.

## Build a simple To-Do app with useState for tasks.

## Implement a modal component that receives content as children.

## Create a reusable button component with props for color, size, and click action.

## Pass a callback from parent to child and trigger it in the child.

## Build a component that renders differently based on prop type (string, array, or object).

## Implement a debounce input using useEffect and setTimeout.

## Use useRef to focus an input when the component mounts.

## Create a timer using useEffect that updates every second.

## Compare useMemo vs useCallback in a small component.

## Build a shopping cart using useReducer.

## Implement a theme switcher using useContext.

## Implement pagination for a list fetched from an API.

## Build a nested comment section (tree structure) using recursion.

## Optimize a large list using React.memo and key props.

## Implement infinite scrolling with API data.

## Measure performance with React.Profiler.    

## Build a protected route (login required) using context or state. Create a "404 Not Found" route.

## Implement nested routes with dynamic parameters.

## Implement navigation with query parameters.

## Build a form with Formik or react-hook-form.

## Add synchronous and asynchronous validation.

## Handle multi-step forms with progress tracking.

## Implement conditional form fields (show/hide based on selection).

## Understand and implement the module pattern.

## Use Map, Set, WeakMap, WeakSet with examples.

## Implement a small data structure (Stack, Queue) in JS.

## Build a higher-order component (HOC).

## Implement a compound component pattern (like Tabs). 

## Implement render props for a reusable component.

## Context with multiple providers.

## Error boundaries to catch rendering errors.

## Fetch data with Axios/Fetch and show loading/error states.

## Use WebSockets or SSE to show real-time data.

## Display data with sorting and filtering after fetching.

## Write a unit test for a component using React Testing Library.

## Snapshot testing for UI components.

## Debug performance issues in a React app.

## Event delegation example in vanilla JS.

## Implement a Promise chain and async/await with error handling.

## Write a function to flatten nested arrays.

## Implement memoization for a Fibonacci function.